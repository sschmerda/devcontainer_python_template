# HTTP template (uncomment to expose plain HTTP directly).
server {
    listen 80;
    server_name _;

    location = /health {
        add_header Content-Type text/plain;
        return 200 'ok\n';
    }

    # Reverse proxy to app running in the dev container on port 8000.
    location / {
        proxy_pass http://dev:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS template (uncomment and provide cert/key files to enable TLS).
# Place cert files under your mounted Nginx config dir, e.g.:
#   devcontainer/services-environment/nginx/certs/dev.crt
#   devcontainer/services-environment/nginx/certs/dev.key
#
# server {
#     listen 443 ssl http2;
#     server_name _;
#
#     ssl_certificate     /etc/nginx/conf.d/certs/dev.crt;
#     ssl_certificate_key /etc/nginx/conf.d/certs/dev.key;
#
#     location = /health {
#         add_header Content-Type text/plain;
#         return 200 'ok\n';
#     }
#
#     location / {
#         proxy_pass http://dev:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
