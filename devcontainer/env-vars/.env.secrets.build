## Secrets template for devcontainer build-time values.
## This file is committed with keys only; set values locally.

## Optional password for user `dev` inside the container.
## Used at image build time via Docker build arg.
## Leave empty to keep current no-password behavior.
DEV_USER_PASSWORD=
