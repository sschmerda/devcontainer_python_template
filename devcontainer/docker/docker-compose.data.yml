services:
  dev:
    volumes:
      - type: bind
        source: ${HOST_DATA_DIR}
        target: ${HOST_DATA_MOUNT_PATH}
        read_only: ${HOST_DATA_READ_ONLY}
        bind:
          create_host_path: false
