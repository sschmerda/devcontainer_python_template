services:
  ## `*_IMAGE_LOCK` values are exported by Make lock targets from
  ## arch-specific keys (`*_IMAGE_LOCK_AMD64` / `*_IMAGE_LOCK_ARM64`)
  ## in services-environment/services-lock.env.
  # postgres:
  #   image: ${POSTGRES_IMAGE_LOCK}

  # mysql:
  #   image: ${MYSQL_IMAGE_LOCK}

  # elasticsearch:
  #   image: ${ELASTICSEARCH_IMAGE_LOCK}

  # redis:
  #   image: ${REDIS_IMAGE_LOCK}

  # minio:
  #   image: ${MINIO_IMAGE_LOCK}

  # nginx:
  #   image: ${NGINX_IMAGE_LOCK}
