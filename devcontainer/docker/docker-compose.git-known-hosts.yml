services:
  dev:
    volumes:
      - type: bind
        source: ${HOST_SSH_KNOWN_HOSTS_PATH}
        target: /home/dev/.ssh/known_hosts
        read_only: true
        bind:
          create_host_path: false
