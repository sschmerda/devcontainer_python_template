services:
  dev:
    environment:
      SSH_AUTH_SOCK: /ssh-agent
    volumes:
      - type: bind
        source: ${HOST_SSH_AUTH_SOCK_PATH}
        target: /ssh-agent
        bind:
          create_host_path: false
